# FPGA-EventCamera
In the present work, a simplified model of an event camera is implemented, featuring a 3 $\times$ 3 matrix of photodiodes that are read out from a 12-channel ADC multiplexer in synchronous mode.
